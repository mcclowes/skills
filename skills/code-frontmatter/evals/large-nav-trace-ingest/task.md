# Trace event ingestion

Trace what happens when a client sends POST /v1/events to the service in `./code`: list the files involved in order, and call out exactly where the event is validated and where it's written to storage. Keep it brief.
