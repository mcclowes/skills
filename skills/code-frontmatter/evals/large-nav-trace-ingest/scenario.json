{
  "description": "Trace POST /v1/events ingestion through a documented analytics-api",
  "fixtures": {
    "codebase": { "type": "directory", "path": "./fixtures/code", "installPath": "code" }
  }
}
