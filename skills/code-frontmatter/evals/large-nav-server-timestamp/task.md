# Find the timestamp-rewriting rule

A customer reports that the event timestamps they send to the service in `./code` are being changed by the server. Which file is responsible, and what is the exact rule it applies to client timestamps? Be specific.
