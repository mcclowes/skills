# Trace a short-link click

In the project at `./code`, trace what happens when a user visits a short link: which files are involved, in order, and where does the click counter get incremented? Keep it brief.
