# Find where to add a backend in a larger service

I need to add a DynamoDB storage backend to the TypeScript service in `./code`. Which file defines the interface my new backend must implement, which existing file is the best template to copy, and which file selects/wires the backend at startup? Answer concisely with file paths.
