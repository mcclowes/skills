{
  "description": "Navigate a documented analytics-api to find where to add a storage backend",
  "fixtures": {
    "codebase": { "type": "directory", "path": "./fixtures/code", "installPath": "code" }
  }
}
