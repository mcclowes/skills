{
  "skill_name": "ai-aware-code-structure",
  "_note": "Eval 0 (split-growing-component) retired in iteration-3: it was non-discriminating (base model scored 4/4 with and without the skill). Eval 0 below (review-over-split) trimmed from 4 to 3 assertions, dropping the redundant 'merge view+logic+types' check the graders flagged. Evals 7-8 (barrel-files, central-types) are new coverage for the placement guidance added to SKILL.md; they're regression guards and may not discriminate, since the base model already gets them right.",
  "evals": [
    {
      "id": 0,
      "name": "review-over-split-feature",
      "prompt": "Review the file structure of this feature folder. We have: UserBadge/index.ts, UserBadge/UserBadge.view.tsx (just JSX), UserBadge/UserBadge.logic.ts (40 lines), UserBadge/UserBadge.styles.ts, UserBadge/types.ts. Each file is small and single-purpose. Is this a good layout for a codebase where we use AI assistants heavily?",
      "expected_output": "Flags that the view file's props are shaped by the logic file, so an agent editing the view lacks context and may make confident breaking changes — the classic smell. Recommends co-locating the tightly coupled view+logic+types, and keeping a split only where each side stands alone (styles being the defensible one).",
      "files": [],
      "assertions": [
        {"text": "Concludes the layout is over-split for an AI-heavy codebase and recommends consolidating the coupled pieces"},
        {"text": "Identifies the dumb-view-props-shaped-by-sibling smell (cross-file context the agent won't fetch)"},
        {"text": "Keeps genuinely isolatable styles as a defensible separate file (does not blanket-merge everything)"}
      ]
    },
    {
      "id": 1,
      "name": "structure-new-module",
      "prompt": "I'm starting a new data-export module from scratch in our Next.js app — it'll handle generating CSVs, kicking off a background job, and polling for status. How should I organise the files so it's easy for both me and Claude to work in?",
      "expected_output": "Advises starting coarser than the old instinct: let files grow to a few hundred lines while each stays one coherent concern, rather than pre-splitting. Reach for an abstraction (well-named function/module with a clear interface) when a part earns one, not a new file for its own sake. Keep tightly coupled code co-located so an agent works from a complete picture; split only when a piece can be understood in isolation or a file passes ~300 lines.",
      "files": [],
      "assertions": [
        {"text": "Advises starting coarser / not pre-splitting the three phases into many files up front"},
        {"text": "Co-locates tightly coupled job lifecycle / polling"},
        {"text": "Recommends an abstraction (function/module) over bare file splits"},
        {"text": "Mentions a ~300-line threshold (or 'a few hundred lines') as the split trigger"}
      ]
    },
    {
      "id": 2,
      "name": "oversized-mixed-utils-should-split",
      "prompt": "We have a single helpers.ts file that's grown to about 550 lines. It's got date formatting functions, a currency formatter, a thin fetch wrapper, a bunch of form-validation predicates, and some array/object utilities. They don't really call each other — it's just where everything landed. It's imported all over the app. I've been reading that big files are actually fine for AI agents now, so should I just leave it as one file?",
      "expected_output": "Should clearly recommend SPLITTING this file. It is past the ~300-line ceiling AND contains multiple genuinely independent concerns (dates, currency, fetch, validation, arrays) that do not need each other for context — each group can be understood in isolation, so the split boundaries are clean. The 'big files are fine for agents' point is being misapplied: that defense is for a single cohesive concern, not a grab-bag. Splitting here raises signal-to-noise. Must NOT advise keeping it whole on anti-reflexive-split grounds.",
      "files": [],
      "assertions": [
        {"text": "Clearly recommends splitting the file (does NOT advise keeping it as one file)"},
        {"text": "Justifies the split by multiple independent concerns AND/OR exceeding the ~300-line ceiling, not blind line count"},
        {"text": "Notes each group (dates/currency/validation/etc.) can be understood in isolation, so the boundaries are clean"},
        {"text": "Corrects the misapplied 'big files are fine for agents' reasoning (that defense is for one cohesive concern, not a grab-bag)"}
      ]
    },
    {
      "id": 3,
      "name": "reusable-pure-component-should-extract",
      "prompt": "We've got basically the same ~50-line Avatar markup (image with fallback initials, presence dot, size variants) copy-pasted inline inside about 12 different components. One of the seniors is arguing we should leave it inline in each file so that when Claude opens any one of those components it sees everything self-contained, rather than chasing an import. The other camp wants to extract a shared <Avatar/> component. Given we lean hard on AI assistants, who's right?",
      "expected_output": "Should recommend EXTRACTING the shared Avatar component. It is purely presentational (props in, JSX out, no side effects) — the textbook 'understood in isolation' unit that costs an agent almost no external context to use. The 'keep inline for self-containment' argument is misapplied: a clean, well-named <Avatar/> is a real abstraction the agent reads at the call site without opening its internals, and 12 divergent copies is a maintenance/consistency hazard. Co-location is for tightly-coupled code, not reusable primitives.",
      "files": [],
      "assertions": [
        {"text": "Recommends extracting the shared Avatar component (NOT keeping 12 inline copies)"},
        {"text": "Grounds it in the pure-presentation / understood-in-isolation test (low external context to consume)"},
        {"text": "Rejects the 'keep inline for self-containment' argument as misapplied to a reusable primitive"},
        {"text": "Frames the shared component as a real abstraction (read at call site without opening internals), not just DRY"}
      ]
    },
    {
      "id": 4,
      "name": "backend-python-fastapi-non-react",
      "prompt": "Python / FastAPI backend, no frontend here. I have an orders.py route module that's about 250 lines: the route handlers, the Pydantic request/response models, the SQLAlchemy queries, and a chunk of discount-calculation business logic. It's growing and I want to organise it so Claude can work in it effectively. How should I lay it out?",
      "expected_output": "Should give advice that GENERALISES beyond React — apply the 'understood in isolation?' test to Python modules. Co-locate the tightly coupled handler + the logic it drives; the discount calculation is a good candidate to pull into a well-named pure function/service (a real abstraction), and the DB/persistence layer can be separated if it stands alone (e.g. a repository). Prefer an abstraction over reflexive file-splitting; ~300-line guidance still applies. Must NOT give React/hook-specific advice ('extract a hook') that is irrelevant to a FastAPI module.",
      "files": [],
      "assertions": [
        {"text": "Applies the reasoning to Python/backend (does NOT give React- or hook-specific advice)"},
        {"text": "Recommends a real abstraction (pure function / service / repository) over reflexive file-splitting"},
        {"text": "Co-locates tightly coupled handler+logic; separates only cleanly-standalone layers (e.g. discount calc or DB access)"},
        {"text": "Applies the understood-in-isolation test and/or the ~300-line guidance to the module"}
      ]
    },
    {
      "id": 5,
      "name": "god-object-class-should-split",
      "prompt": "I have a UserService TypeScript class that's about 420 lines. It handles auth-token issuing and validation, profile CRUD against the database, sending transactional emails, and firing analytics events. It's all in one file. My reasoning was that keeping it together means Claude has everything in one place and never has to go hunting. Is that the right call?",
      "expected_output": "Should recommend SPLITTING the god object by responsibility. It is past ~300 lines AND bundles four genuinely independent concerns (auth, persistence, email, analytics) that do not need each other's internals — each seam can be understood in isolation, so the boundaries are clean. The 'everything in one place for the agent' instinct is misapplied here: at 420 lines of unrelated responsibilities, signal-to-noise is the problem, and a focused auth/email/etc. module is a better prompt. Co-location is for coupled code, not a kitchen-sink class.",
      "files": [],
      "assertions": [
        {"text": "Recommends splitting the class by responsibility (does NOT endorse keeping all 4 concerns in one file)"},
        {"text": "Identifies the seams (auth / persistence / email / analytics) as genuinely independent and isolatable"},
        {"text": "Cites the ~300-line ceiling AND/OR multiple-unrelated-concerns as the reason, not blind line count"},
        {"text": "Pushes back on the 'keep everything in one place for the agent' rationale as misapplied to a god object"}
      ]
    },
    {
      "id": 6,
      "name": "keep-clean-abstraction-dont-inline",
      "prompt": "Review question. We have pricing/calculatePrice.ts — a pure function, ~120 lines, fully typed inputs and outputs, with its own calculatePrice.test.ts. It's imported by 6 different call sites. A teammate wants to inline it back into the main CheckoutSummary component 'so Claude sees the whole pricing flow in one place when it's editing checkout.' We use AI assistants a lot. Is inlining a good move here?",
      "expected_output": "Should recommend KEEPING calculatePrice as its own file and reject inlining. It is a genuine abstraction with a clean, typed interface — the agent reads the call site (typed inputs/outputs) and proceeds without needing the internals, which is exactly the 'understood in isolation' win. Inlining would bloat CheckoutSummary, lose the isolated test, and duplicate logic across 6 callers. The skill explicitly prefers real abstractions over relocating/inlining code. This tests that the skill doesn't over-rotate into 'always co-locate / inline so the agent sees everything.'",
      "files": [],
      "assertions": [
        {"text": "Recommends keeping calculatePrice as its own file (rejects inlining it into the component)"},
        {"text": "Grounds it in the abstraction-over-file-structure heuristic (clean typed interface, hides implementation)"},
        {"text": "Notes the agent uses the typed call site without needing the internals (understood in isolation)"},
        {"text": "Rejects the 'inline so Claude sees it all' argument for a clean, reused, tested abstraction"}
      ]
    },
    {
      "id": 7,
      "name": "barrel-files-kill-reflexive",
      "prompt": "Our React + TypeScript app re-exports everything through barrel files: every folder has an index.ts that re-exports its contents, and there's a big src/components/index.ts too. Imports look like `import { Button, Modal, Card } from '@/components'`. We now do almost all our editing through Claude Code and Cursor. Is the barrel-file pattern helping or hurting us, and would you keep it?",
      "expected_output": "Should recommend dropping the reflexive per-folder barrels and the big aggregator in favour of direct path imports, while keeping a deliberate barrel only at a real public-API/package boundary. Grounds it in agent indirection: the barrel hides where a symbol lives, so the agent hops through a noisy re-export wall or guesses the shape and edits wrong. Bonus: names a concrete secondary cost (circular imports, broken tree-shaking, slow build/test startup). Must NOT say 'delete all barrels' indiscriminately.",
      "files": [],
      "assertions": [
        {"text": "Recommends dropping the reflexive per-folder and big aggregator barrels in favour of direct path imports"},
        {"text": "Grounds it in agent indirection: the barrel hides where a symbol lives, so the agent hops or guesses the shape"},
        {"text": "Keeps a deliberate barrel only at a real public-API / package boundary (does NOT say delete all barrels)"},
        {"text": "Names at least one concrete secondary cost (circular imports, tree-shaking, or slow build/test startup)"}
      ]
    },
    {
      "id": 8,
      "name": "central-types-colocate",
      "prompt": "We keep all our shared TypeScript types in one src/types.ts file. It's about 400 lines now and basically every component and service imports from it. It felt clean to have a single source of truth. We do most of our editing through Claude Code now. Is a central types file a good idea, or should types live somewhere else?",
      "expected_output": "Should recommend co-locating a type with the module that owns it as the default, lifting into a shared/feature-level file only when several modules genuinely share it, and keeping the handful of truly global primitives central. Reframes 'single source of truth' as 'defined once, not all in one file'. Grounds it in the agent not opening the sibling: a central types file is the cross-file lookup the agent skips, so it edits the code and forgets the type. Must NOT advise one-type-per-file scattering.",
      "files": [],
      "assertions": [
        {"text": "Recommends co-locating a type with the module that owns it as the default (not a central types.ts)"},
        {"text": "Reframes 'single source of truth' as defined-once, not all-in-one-file"},
        {"text": "Keeps genuinely shared/global types in a shared or feature-level file (does NOT advise one-type-per-file scattering)"},
        {"text": "Grounds it in the agent skipping the cross-file lookup (the central types file is the sibling it won't open)"}
      ]
    }
  ]
}
